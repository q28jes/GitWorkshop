git clone git@github.com:username/repository.git
cloneไฟล์ก่อน

git remote set-url origin git@github.com:Fres26/pre-midterm-practice-Fres26.git 

git remote -v
ต้องเห็นเป็น SSH ของ Fres26 ทั้ง fetch / push

git push -u origin main ได้เลย

clone repo อจ เข้าrepoเรา
